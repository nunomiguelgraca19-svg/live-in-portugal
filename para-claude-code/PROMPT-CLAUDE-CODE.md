# Prompt for Claude Code — Portugal Expat Home

> **Copy everything between the `---` lines below and paste into a Claude Code session opened in the `para-claude-code/` directory.**

---

In `Assistente-Onde-Morar-Portugal.html`, please make three replacements:

1. **`places` array** → replace with the array in `concelhos-corrigido.js`.
2. **`schools` array** → replace with the entries listed in `Lista-Escolas-Internacionais-Atualizada.md`.
3. **`scorePlace()` function (and the helper `scoreInternationalSchools` that follows it)** → replace with the contents of `scorePlace-corrigido.js`.

Make a backup first (`Assistente-Onde-Morar-Portugal.bak.html`). Don't deploy — Nuno will review the result.
